apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "train-api.fullname" . }}-test-connection"
  labels:
    {{- include "train-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "train-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
